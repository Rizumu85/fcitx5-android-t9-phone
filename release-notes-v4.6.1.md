## 新功能

- 新增无感九键词库部署。安装输入法本体和 Rime 插件后，会自动下载、校验并部署当前版本匹配的九键配置，不再要求手动复制文件、重新部署、同步或切换系统输入法。
- 新增【完整物理按键音】。授权轻量无障碍服务后，输入法未打开时的返回、菜单、接听和挂断键也能使用所选按键音；服务不会读取屏幕内容或拦截按键。
- 应用内更新现在统一管理输入法本体、Rime 插件和九键 Rime 配置，并能自动修复缺失或损坏的配置。

## 体验与修复

- 大幅改进中文九键连续输入时的响应速度，减少候选内容与上一帧差异较大时的迟滞、重复刷新和短暂旧候选。
- 修复长句拼音预览、首轮候选加载、候选焦点边距及气泡左右留白在部分内容下不一致的问题。
- 修复 Rime 进程重启后输入状态丢失，以及切换拼音、笔画、注音后长期停留在【中文词库准备中】的问题。
- 切换拼音、笔画和注音时只显示一次方案提示，不再把正常切换误报为词库部署，也不会因 Rime 的重复状态名称重新刷新界面。
- 修复手写 ML Kit 模型下载失败后点击重试没有反应的问题。
- 修复正式版压缩后 ML Kit 组件注册器被错误移除，导致中英文手写模型始终下载失败的问题。
- Rime 配置安装改为事务式部署；下载失败、网络中断或部署异常不会破坏原本可用的词库。

## 安装包

本次 Release 提供输入法本体和 Rime 插件，并分别提供 32 位与 64 位版本：

- 32 位手机下载 `armeabi-v7a` 版本。
- 64 位手机下载 `arm64-v8a` 版本。

同一架构的输入法本体和 Rime 插件两个 APK 都需要安装。例如 64 位手机需要安装：

- `org.fcitx.fcitx5.android-4.6.1-arm64-v8a-release.apk`
- `org.fcitx.fcitx5.android.plugin.rime-4.6.1-arm64-v8a-release.apk`

安装完成后打开输入法本体并按引导启用。首次进入文本框时保持联网，匹配的九键配置会自动下载和部署。

## Rime 版本对应关系

- Rime 插件：`4.6.1`，请与输入法本体一起更新。
- 九键 Rime 配置：[rime-ice-t9-phone v3.1.0](https://github.com/Rizumu85/rime-ice-t9-phone/releases/tag/v3.1.0)。

本次九键配置已从 v3.0.0 更新到 v3.1.0。安装 4.6.1 后会自动下载、校验和部署 v3.1.0；已经使用 v3.1.0 的用户会直接复用，不会重复部署。
