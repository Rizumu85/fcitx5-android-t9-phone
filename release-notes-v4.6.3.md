## 新功能

- 新增【联想中文】。提交中文候选后可继续选择下一段文字，并支持方向键、确认键、短按 `0` 和长按数字快捷选择；退格会先关闭联想候选，不会误删已经输入的文字。
- 新增中文自定义词库，可为自定义中文词语填写无声调拼音，并在拼音九键候选中优先使用。
- 新增拼音英文词库。可单独管理中文模式下出现的英文候选，也可与【联想英文词库】合并共享。
- 使用 `#` 提交同一段拼音字母两次后，可自动学习为拼音英文候选，方便录入词库中没有的英文短词。
- 重新整理【输入方式】中的联想与词库设置，并统一“联想英文”等相关名称。

## 修复与优化

- 修复普通打开输入法、系统恢复进程或切换中文方案时，偶尔误显示【中文词库正在准备】的问题。普通 Rime 启动现在与真正的词库部署分开处理。
- 输入法启动期间立即按下的实体键会继续排队处理；空闲启动保持安静，只有用户确实正在等待输入时才显示准备状态。
- 修复简体模式的联想中文偶尔出现繁体候选，以及联想状态下残留拼音筛选行的问题。
- 缩小检查更新页面的下载操作区域，让多个版本的更新说明有更多可见空间。

## 安装包

本次 Release 提供输入法本体和 Rime 插件，并分别提供 32 位与 64 位版本：

- 32 位手机下载 `armeabi-v7a` 版本。
- 64 位手机下载 `arm64-v8a` 版本。

同一架构的输入法本体和 Rime 插件两个 APK 都需要更新。例如 64 位手机需要安装：

- `org.fcitx.fcitx5.android-4.6.3-arm64-v8a-release.apk`
- `org.fcitx.fcitx5.android.plugin.rime-4.6.3-arm64-v8a-release.apk`

覆盖安装会保留设置和用户词库。安装后输入法会自动下载、校验并部署本版匹配的九键 Rime 配置。

## Rime 版本对应关系

- Rime 插件：`4.6.3`。本次插件区分普通引擎启动与真正部署，必须与输入法本体一起更新。
- 九键 Rime 配置：[rime-ice-t9-phone v3.2.1](https://github.com/Rizumu85/rime-ice-t9-phone/releases/tag/v3.2.1)。

4.6.2 使用的配置为 v3.1.0，本次需要更新到 v3.2.1。保持联网后输入法会自动完成更新和一次必要的部署；国内网络无法连接 GitHub 时，也可以使用百度网盘文件夹附带的 `rime-ice-t9-phone-main.zip` 手动恢复。
