## 新功能

- 拼音九键现在支持直接连续输入数字键默认显示的声母来组句，无需逐个进入拼音筛选。
- 手动确认非默认声母以及 `zh`、`ch`、`sh` 后，也可以继续组合任意简拼。

## 修复与优化

- 修复连续输入多个默认声母时可能完全没有汉字候选的问题；普通完整拼音仍走原有高速路径。
- 修复候选行明明还有可用宽度，却提前把能放下的候选移到下一页的问题。
- 默认声母候选的上方预览现在只显示实际输入的声母，不再混入尚未选择的完整拼音字母。

## 安装包

本次 Release 提供输入法本体和 Rime 插件，并分别提供 32 位与 64 位版本：

- 32 位手机下载 `armeabi-v7a` 版本。
- 64 位手机下载 `arm64-v8a` 版本。

同一架构的输入法本体和 Rime 插件两个 APK 都需要更新。例如 64 位手机需要安装：

- `org.fcitx.fcitx5.android-4.6.5-arm64-v8a-release.apk`
- `org.fcitx.fcitx5.android.plugin.rime-4.6.5-arm64-v8a-release.apk`

覆盖安装会保留设置和用户词库。

## Rime 版本对应关系

- Rime 插件：`4.6.5`，请与输入法本体一起更新。
- 九键 Rime 配置：[rime-ice-t9-phone v3.2.2](https://github.com/Rizumu85/rime-ice-t9-phone/releases/tag/v3.2.2)。

本次修改了拼音九键配置。升级后输入法会自动下载、校验并无感部署 v3.2.2；国内网络无法访问 GitHub 时，也可以从 Release 或百度网盘手动取得对应配置包。
